{% extends "base.njk" %}
{% block titulo %}Listado de limpiezas{% endblock %}
{% block contenido %}
    <h1 class="fw-semibold">Limpiezas de habitación {{ habitacion.numero }}</h1>
    <div class="row g-2 mb-2">
        <div class="col">
            <a href="/limpiezas/nueva/{{ habitacion.id }}" class="btn btn-primary">Nueva limpieza</a>
            <a href="/habitaciones/{{ habitacion.id }}" class="btn btn-secondary">Volver</a>
        </div>
    </div>
    <div class="row g-2 mb-2">
        {% for limpieza in limpiezas %}
            <div class="col-12">
                <div class="card">
                    <div class="card-body">
                        <p class="card-text fst-italic">{{ limpieza.fechaHora | date("DD/MM/YYYY") }}</p>
                        {% if limpieza.observaciones %}
                            <p class="card-text">{{ limpieza.observaciones }}</p>
                        {% else %}
                            <p class="card-text fst-italic">Sin observaciones</p>
                        {% endif %}
                    </div>
                </div>
            </div>
        {% else %}
            <p>No hay limpiezas</p>
        {% endfor %}
    </div>
{% endblock %}